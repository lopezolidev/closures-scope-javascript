pi = 3.1416;
console.log(pi); // 3.1416

'use strict';
e = 2.718; // assigned
console.log(e); // ReferenceError: e is not defined

// Functions

function myFunction(){
   return µ = 0.00006; // assigned 
}

console.log(myFunction()); // 0.00006

function anotherFunc() {
    'use strict';
    return µ = 0.00006; // assigned
}

console.log(anotherFunc()); // ReferenceError: µ is not defined

//using 'use strict'; means that we'll have to declare first a variable to make use of it, instead of only assigning